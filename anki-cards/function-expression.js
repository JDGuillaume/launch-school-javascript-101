// The following is an example of a function expression.
// We can use the function keyword inside an expression to define a function.

const getRectArea = function(width, height) {
  return width * height;
};
